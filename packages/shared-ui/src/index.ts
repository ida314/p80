/**
 * Shared web components. Empty in Stage 1 by design.
 *
 * ADR 0007: there is no UI-abstraction layer — two concrete clients over one HTTP API,
 * not a client framework. This package is for components shared between *web* surfaces,
 * and there is only one web surface until Stage 2.
 */
export {};
